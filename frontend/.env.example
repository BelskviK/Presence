# Base URL of the backend API.
# Vite only exposes variables prefixed with VITE_ to the browser.
# Defaults to http://localhost:5000/api if this file/variable is absent.
VITE_API_URL=http://localhost:5000/api
